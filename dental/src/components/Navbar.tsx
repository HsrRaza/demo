"use client";
import { MapPin, Menu, Phone, Sparkles, X } from "lucide-react";
import { useState } from "react";
export default function Navbar({ onBook }: { onBook: () => void }) { const [open, setOpen] = useState(false); const links = [["Services", "#services"], ["Doctors", "#doctors"], ["Why Us", "#why-us"], ["Reviews", "#reviews"], ["Contact", "#contact"]]; return <><div className="bg-sky-700 text-white"><div className="container flex h-9 items-center justify-between text-[11px] font-semibold tracking-wide sm:text-xs"><span className="flex items-center gap-2"><Phone size={13} /> Emergency: +1 (555) 321-4321</span><span className="hidden items-center gap-2 sm:flex"><MapPin size={13} /> 245 Park Avenue, New York</span></div></div><header className="sticky top-0 z-40 border-b border-slate-100/80 bg-white/90 backdrop-blur-lg"><div className="container flex h-[76px] items-center justify-between"><a href="#" className="flex items-center gap-2.5 text-slate-900"><span className="grid h-10 w-10 place-items-center rounded-xl bg-sky-600 text-white shadow-lg shadow-sky-200"><Sparkles size={20}/></span><span className="text-[15px] font-extrabold tracking-tight sm:text-base">Lumina <span className="font-medium text-slate-500">Dental Studio</span></span></a><nav className="hidden items-center gap-7 lg:flex">{links.map(([label, href]) => <a key={label} href={href} className="text-sm font-semibold text-slate-600 transition hover:text-sky-600">{label}</a>)}</nav><button onClick={onBook} className="hidden rounded-xl bg-sky-600 px-5 py-3 text-sm font-bold text-white transition hover:bg-sky-700 sm:block">Book Appointment</button><button aria-label="Toggle navigation" className="grid h-10 w-10 place-items-center rounded-lg text-slate-700 lg:hidden" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></div>{open && <div className="border-t border-slate-100 bg-white px-5 py-5 lg:hidden"><nav className="container flex flex-col gap-4">{links.map(([label, href]) => <a onClick={() => setOpen(false)} key={label} href={href} className="font-bold text-slate-700">{label}</a>)}<button onClick={() => { setOpen(false); onBook(); }} className="rounded-xl bg-sky-600 py-3 font-bold text-white">Book Appointment</button></nav></div>}</header></>; }
