"use client";
import { CheckCircle2, ChevronDown, X } from "lucide-react";
import { FormEvent, useState } from "react";
type Props = { isOpen: boolean; onClose: () => void };
const field = "mt-1.5 w-full rounded-lg border border-slate-200 bg-white px-3.5 py-2.5 text-sm text-slate-700 outline-none transition placeholder:text-slate-400 focus:border-sky-500 focus:ring-3 focus:ring-sky-100";
export default function AppointmentModal({ isOpen, onClose }: Props) { const [submitted, setSubmitted] = useState(false); const [error, setError] = useState(""); if (!isOpen) return null; function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const data = new FormData(e.currentTarget); if (!data.get("name") || !data.get("phone") || !data.get("email") || !data.get("service") || !data.get("date")) { setError("Please complete the required fields so we can reserve your visit."); return; } setError(""); setSubmitted(true); } function close(){setSubmitted(false);setError("");onClose();} return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-label="Book an appointment"><div className="relative max-h-[92vh] w-full max-w-xl overflow-y-auto rounded-3xl bg-white p-6 shadow-2xl sm:p-8"><button aria-label="Close booking form" onClick={close} className="absolute right-5 top-5 grid h-9 w-9 place-items-center rounded-full bg-slate-100 text-slate-500 hover:bg-slate-200"><X size={18}/></button>{submitted ? <div className="py-14 text-center"><CheckCircle2 className="mx-auto text-teal-500" size={60}/><h2 className="mt-5 font-serif text-3xl font-semibold text-slate-900">You’re all set!</h2><p className="mx-auto mt-3 max-w-sm text-sm leading-6 text-slate-500">Thanks for choosing Lumina. Our care coordinator will call you shortly to confirm your appointment.</p><button onClick={close} className="mt-8 rounded-xl bg-sky-600 px-6 py-3 text-sm font-bold text-white">Back to Lumina</button></div> : <><p className="text-xs font-extrabold uppercase tracking-[.15em] text-sky-600">A brighter visit begins here</p><h2 className="mt-2 font-serif text-3xl font-semibold text-slate-900">Book an appointment</h2><p className="mt-2 text-sm text-slate-500">Tell us a little about your needs. We’ll confirm your preferred time shortly.</p><form onSubmit={submit} className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-slate-700">Full name *<input name="name" className={field} placeholder="Your full name"/></label><label className="text-xs font-bold text-slate-700">Phone number *<input name="phone" type="tel" className={field} placeholder="(555) 000-0000"/></label><label className="text-xs font-bold text-slate-700">Email address *<input name="email" type="email" className={field} placeholder="you@email.com"/></label><label className="relative text-xs font-bold text-slate-700">Preferred service *<select name="service" defaultValue="" className={`${field} appearance-none`}><option value="" disabled>Select a service</option><option>General Dentistry</option><option>Cosmetic Dentistry</option><option>Orthodontics & Aligners</option><option>Restorative Care</option><option>Pediatric Dentistry</option><option>Emergency Care</option></select><ChevronDown className="pointer-events-none absolute bottom-3 right-3 text-slate-400" size={16}/></label><label className="relative text-xs font-bold text-slate-700">Preferred doctor <select name="doctor" defaultValue="" className={`${field} appearance-none`}><option value="">No preference</option><option>Dr. Maya Chen</option><option>Dr. Daniel Ross</option></select><ChevronDown className="pointer-events-none absolute bottom-3 right-3 text-slate-400" size={16}/></label><label className="text-xs font-bold text-slate-700">Preferred date *<input name="date" type="date" className={field}/></label><label className="sm:col-span-2 text-xs font-bold text-slate-700">Message or notes<textarea name="notes" rows={3} className={`${field} resize-none`} placeholder="Anything you'd like us to know?"/></label>{error && <p className="sm:col-span-2 rounded-lg bg-rose-50 p-3 text-xs font-semibold text-rose-600">{error}</p>}<button className="sm:col-span-2 rounded-xl bg-sky-600 py-3.5 text-sm font-bold text-white transition hover:bg-sky-700">Request appointment</button></form><p className="mt-4 text-center text-[11px] text-slate-400">By submitting, you agree to be contacted by Lumina Dental Studio.</p></>}</div></div>; }
